.modal-header
    button.close(ng-click='cancel()') &times;
    h3.text-warning {{msg.title || "Status" }}
.modal-body
    h5(style="white-space: pre;") {{msg.msg}}
.modal-footer
    button.btn.btn-warning(ng-click='cancel()') Dismiss