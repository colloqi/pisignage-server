.modal-header
    button.close(ng-click='modal.dismiss()') &times;
    h3.text-danger(translate='translate') Software Update
.modal-body
    h5(ng-if="msg.player.version == currentVersion.version")
        b(translate='translate') Your player Up-to-Date
    h5(ng-if="msg.player.version != currentVersion.version")
        span(translate='translate') Do you want to update Software from 
        | {{msg.curVer}} 
        span(translate='translate') to 
        | {{msg.newVer}}
.modal-footer
    button.btn.btn-xs.btn-link.pull-left(ng-if="currentVersion.beta && msg.player.version != currentVersion.beta",
                ng-click='confirmUpdate("piimage-beta.zip")')
        em.text-warning(translate='translate') Update to beta 
        em.text-warning &nbsp;{{currentVersion.beta}}
    button.btn(ng-click='modal.dismiss()',translate='translate') Cancel
    button.btn.btn-danger(ng-click='confirmUpdate()',translate='translate') OK

